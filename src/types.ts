export type Team={id:string;name:string;club:string;pool:string;color?:string;category?:string;contact?:string;checkedIn?:boolean;changingRoom?:string}
export type MatchStatus='scheduled'|'live'|'finished'
export type MatchStage='pool'|'quarter'|'semi'|'final'|'third'
export type Role='owner'|'admin'|'scorekeeper'|'viewer'
export type Match={id:string;kickoff:string;field:string;homeTeamId:string|null;awayTeamId:string|null;homeScore:number|null;awayScore:number|null;status:MatchStatus;pool:string;referee?:string;stage?:MatchStage;homeSource?:string|null;awaySource?:string|null;bracketCode?:string|null;duration?:number;delay?:number}
export type Division={id:string;name:string;poolCount:number;maxTeams:number;knockout:boolean;thirdPlace:boolean}
export type Settings={matchMinutes:number;switchMinutes:number;restMinutes:number;publicMessage:string;tvEnabled:boolean;pointsWin:number;pointsDraw:number;startTime:string;address:string;clubName:string;showSponsors:boolean;showFavorites:boolean;advancePerPool:number}
export type Registration={id:string;teamName:string;clubName:string;category:string;contactName:string;contactEmail:string;note:string;status:'pending'|'approved'|'rejected'}
export type Notice={id:string;title:string;body:string;createdAt:string}
export type AuditEntry={id:string;action:string;entity:string;detail:string;createdAt:string}
export type Invitation={id:string;email:string;role:Exclude<Role,'owner'>;acceptedAt:string|null}
export type Standing={team:Team;played:number;won:number;drawn:number;lost:number;goalsFor:number;goalsAgainst:number;points:number}
export type Field={id:string;name:string;surface:string;status:'open'|'paused'|'closed'}
export type Official={id:string;name:string;role:string;availability:string;matches:number}
export type Volunteer={id:string;name:string;task:string;shift:string;status:'confirmed'|'invited'|'needed'}
export type Sponsor={id:string;name:string;contact:string;stage:'Nieuw'|'Benaderen'|'Gesprek'|'Voorstel'|'Gewonnen';score:number;value:number;package:string;nextAction:string}
export type Tournament={id:string;name:string;date:string;eventDate:string;venue:string;published:boolean;teams:Team[];matches:Match[];fields?:Field[];officials?:Official[];volunteers?:Volunteer[];sponsors?:Sponsor[];settings?:Settings;divisions?:Division[];registrations?:Registration[];notices?:Notice[];audit?:AuditEntry[];invitations?:Invitation[];role?:Role|null}
